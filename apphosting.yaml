# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Minimum instances to avoid cold starts
  minInstances: 1
  # Maximum instances to handle concurrent traffic
  maxInstances: 10
