
{
  "entities": {
    "Tournament": {
      "title": "Tournament",
      "description": "Global tournament registry. PERMISSIONS: Publicly readable. Writable by club owner. Supports multiple days and locations.",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "name": { "type": "string" },
        "startDate": { "type": "string", "format": "date" },
        "endDate": { "type": "string", "format": "date" },
        "locations": { "type": "array", "items": { "type": "string" } },
        "sport": { "type": "string" },
        "status": { "type": "string", "enum": ["draft", "active", "finished"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "description": { "type": "string" },
        "numCourts": { "type": "number" },
        "categories": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "format": { "type": "string" },
              "sets": { "type": "number" },
              "ageGroup": { "type": "string" },
              "isTeamBased": { "type": "boolean" }
            }
          }
        }
      },
      "required": ["clubId", "name", "startDate", "sport", "status"]
    },
    "Match": {
      "title": "Match",
      "description": "Match records. Publicly readable. Writable by club owner.",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "tournamentId": { "type": "string" },
        "court": { "type": "number" },
        "category": { "type": "string" },
        "teamA": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "score": { "type": "number" },
            "setsWon": { "type": "number" },
            "players": { "type": "array", "items": { "type": "string" } }
          }
        },
        "teamB": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "score": { "type": "number" },
            "setsWon": { "type": "number" },
            "players": { "type": "array", "items": { "type": "string" } }
          }
        },
        "status": { "type": "string", "enum": ["scheduled", "live", "completed"] },
        "startTime": { "type": "string", "format": "date-time" },
        "completedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["clubId", "tournamentId", "status"]
    },
    "Club": {
      "title": "Club",
      "description": "Club profiles. Publicly readable. Writable by ownerId.",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "name": { "type": "string" },
        "location": { "type": "string" },
        "contactEmail": { "type": "string" },
        "numCourts": { "type": "number" },
        "primarySport": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerId", "name"]
    },
    "User": {
      "title": "User",
      "description": "User profile. Admin (admin@deneme.com) has global read access.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string" },
        "clubId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Participant": {
      "title": "Participant",
      "description": "Tournament entries. Publicly readable. Writable by participant or owner.",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "tournamentId": { "type": "string" },
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "telegramHandle": { "type": "string" },
        "skillLevel": { "type": "string" },
        "categoryId": { "type": "string" },
        "categoryName": { "type": "string" },
        "registeredAt": { "type": "string", "format": "date-time" }
      },
      "required": ["clubId", "tournamentId", "name", "email"]
    },
    "CheckIn": {
      "title": "Check-In",
      "description": "Arrival confirmation at venue. PERMISSIONS: Publicly readable and listable. Writable by any authenticated user for their own record or by club owner.",
      "type": "object",
      "properties": {
        "participantId": { "type": "string" },
        "tournamentId": { "type": "string" },
        "location": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "date": { "type": "string", "format": "date" }
      },
      "required": ["participantId", "tournamentId", "timestamp"]
    },
    "Sponsor": {
      "title": "Sponsor",
      "description": "Partners. Publicly readable. Writable by club owner.",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "name": { "type": "string" },
        "websiteUrl": { "type": "string" },
        "tier": { "type": "string", "enum": ["gold", "silver", "bronze"] },
        "logoUrl": { "type": "string" }
      },
      "required": ["clubId", "name", "tier"]
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "/tournaments/{tournamentId}": { "schema": "Tournament", "description": "Public tournament registry. Writable by club owner." },
    "/matches/{matchId}": { "schema": "Match", "description": "Match scores. Writable by club owner." },
    "/clubs/{clubId}": { "schema": "Club", "description": "Organizations. Writable by ownerId." },
    "/users/{userId}": { "schema": "User", "description": "Private profile. Writable by userId." },
    "/participants/{participantId}": { "schema": "Participant", "description": "Registrations. Publicly readable. Writable by participant or club owner." },
    "/checkins/{checkInId}": { "schema": "CheckIn", "description": "Venue arrivals. Publicly readable. Writable by club owner." },
    "/sponsors/{sponsorId}": { "schema": "Sponsor", "description": "Partnership info. Writable by club owner." }
  }
}
