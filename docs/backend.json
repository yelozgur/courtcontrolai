
{
  "entities": {
    "Tournament": {
      "title": "Tournament",
      "description": "A sports tournament entity",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "name": { "type": "string" },
        "startDate": { "type": "string", "format": "date" },
        "sport": { "type": "string" },
        "status": { "type": "string", "enum": ["draft", "active", "finished"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "description": { "type": "string" },
        "imageUrl": { "type": "string" }
      },
      "required": ["clubId", "name", "startDate", "sport", "status"]
    },
    "Match": {
      "title": "Match",
      "description": "An individual game or match in a tournament",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "tournamentId": { "type": "string" },
        "court": { "type": "number" },
        "category": { "type": "string" },
        "teamA": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "score": { "type": "number" },
            "players": { "type": "array", "items": { "type": "string" } }
          }
        },
        "teamB": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "score": { "type": "number" },
            "players": { "type": "array", "items": { "type": "string" } }
          }
        },
        "status": { "type": "string", "enum": ["scheduled", "live", "completed"] },
        "startTime": { "type": "string", "format": "date-time" },
        "durationMinutes": { "type": "number" }
      },
      "required": ["clubId", "tournamentId", "court", "status"]
    },
    "Club": {
      "title": "Club",
      "description": "Information about the sports club",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "name": { "type": "string" },
        "location": { "type": "string" },
        "contactEmail": { "type": "string", "format": "email" },
        "numCourts": { "type": "number" },
        "logoUrl": { "type": "string" }
      },
      "required": ["ownerId", "name", "contactEmail"]
    },
    "Participant": {
      "title": "Participant",
      "description": "A player registered for tournaments",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "tournamentId": { "type": "string" },
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "telegramHandle": { "type": "string" },
        "skillLevel": { "type": "string", "enum": ["beginner", "intermediate", "pro"] },
        "registeredAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "clubId", "tournamentId"]
    },
    "Sponsor": {
      "title": "Sponsor",
      "description": "A tournament or club sponsor",
      "type": "object",
      "properties": {
        "clubId": { "type": "string" },
        "name": { "type": "string" },
        "logoUrl": { "type": "string" },
        "websiteUrl": { "type": "string" },
        "tier": { "type": "string", "enum": ["gold", "silver", "bronze"] }
      },
      "required": ["clubId", "name", "tier"]
    },
    "User": {
      "title": "User",
      "description": "Application user record",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["admin", "user"] },
        "clubId": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/tournaments/{tournamentId}": {
      "schema": "Tournament",
      "description": "Collection of tournaments"
    },
    "/matches/{matchId}": {
      "schema": "Match",
      "description": "Collection of individual matches"
    },
    "/clubs/{clubId}": {
      "schema": "Club",
      "description": "Club profile information"
    },
    "/participants/{participantId}": {
      "schema": "Participant",
      "description": "Global list of club participants"
    },
    "/sponsors/{sponsorId}": {
      "schema": "Sponsor",
      "description": "List of club sponsors"
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles and roles"
    }
  }
}
